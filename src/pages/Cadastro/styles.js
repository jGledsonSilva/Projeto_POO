const background = {
    display: 'flex',
    height: '100vh',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#0f0f0f',
}

const box = {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100px',
    width: '240px',
}

const paper = {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '8px 32px',
    backgroundColor: '#222222',
}

const title = {
    fontWeight: "700",
    color: '#ffffff'
}


const a = {
    fontWeight: "500",
    color: '#ffffff'
}

const button = {
    borderRadius: '100px'
}


const titleColor = {
    display: "inline",
    fontWeight: "200",
    color: "#4DCBD3"
  }
  
  const logoTitle = {
    display: "inline",
    fontWeight: "200",
    color: "#ffffff"
  }
export const Styles ={
    titleColor,
    logoTitle,
    background,
    button,
    title,
    paper,
    box,
    a,
}