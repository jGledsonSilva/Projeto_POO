const background = {
    display: 'flex',
    height: '100vh',
    justifyContent: 'center',
    alignItems: 'center',
}

const conteiner = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
}

const paper = {
    height: '300px',
    width: '450px',
    backgroundColor: '#3a3a3a',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
}

const h1 = {
    color: '#ffffff'
}

export const Styles ={
    background,
    conteiner,
    paper,
    h1,
}