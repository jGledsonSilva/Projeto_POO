const buttonsBox = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  gap: '2rem'
}

const sideButtons = {
  width: '52px'
}

const midButtons = {
  width: '96px'
}

export const styles = {
  buttonsBox,
  midButtons,
  sideButtons
}
