import { createTheme } from '@mui/material'

export const Testtheme = createTheme({
    palette: {
         mode: 'dark',

        background: {
            default: ' #141414',
            paper: '#000000'
        }
    },
})